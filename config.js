/**
 * The only reason for using a *.js file for
 * config is that I'd frequently comment out
 * keys in the config. Hopefully I'd get a
 * remedy for this very soon!
 */

module.exports = {
  port: 3030
};
